type: object
required:
  - name
  - countryCode
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
  countryCode:
    type: string
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
